node_modules/
.DS_Store
Thumbs.db
*.log

# Cloudflare Wrangler
.wrangler/
.dev.vars

# Local env
.env
.env.local

# IDE
.vscode/
.idea/

# Company workspace (private secretary notes)
.company/

# .assetsignore handles excluding originals/ from the Workers upload,
# so we keep originals/ in git (source assets, ~10MB) but they don't get deployed.
